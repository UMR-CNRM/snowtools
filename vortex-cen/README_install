# Utiliser pytohn 3.10.12
# TODO : comprendre le problème avec python/3.12.12
module load python/3.10.12


# I. Création d'un environnement virtuel AVEC les packages systems
# WARNING : do not use --system-site-packages, this can lead to issues with pyproj because it is natively installed with conda:
python -m venv $HOME/venv/vortex2
source $HOME/venv/vortex2/bin/activate

# II. Installation de snowtools
#module load gcc/14.1.0
module load gcc
# Install snowtools as non editable first to avoid issues
# TODO : uderstand the problem and fix it properly
install_snowtools
install_snowtools -e
#install_snowtools -v {path_to_venv}
#source {path_to_venv}/bin/activate


# III. Installation manuelle des dépendances internes (mkjob, vortrex-gco, vortex-olive)
# Ces packages sont disponibles sur Nexus qui n'est actuellemnt (10/02/2026) pas accessible depuis belenos
# Une demande de décloisonnement a été faite par Thibault.L
# En attendant, suivre les étapes suivantes :

# 1. Configurer gitlab si besoin : http://confluence.meteo.fr/display/~thomas.carrel-billiard@meteo.fr/Configurer+GitLab

# 2. Cloner les dépots :
cd {any_path}
git clone git@gitlab.meteo.fr:cnrm-gmap/mkjob.git
git clone git@gitlab.meteo.fr:cnrm-gmap/vortex-gco.git
git clone git@gitlab.meteo.fr:cnrm-gmap/vortex-olive.git

# 3. Installer les dépots :
pip install mkjob/ vortex-gco/ vortex-olive/


# IV Installation de vortex-cen
#pip install vortex-nwp
pip install --upgrade setuptools
#pip install --upgrade packaging
cd $SNOWTOOLS_CEN/vortex-cen

# Install vortex-cen as non editable first to avoid issues
# TODO : uderstand the problem and fix it properly
pip install .
pip install --no-build-isolation -e .

# V. Configuration de vortex (1ère fois uniquement)
mkdir $HOME/.vortex.d/
cd $HOME/.vortex.d/
ln -s $SNOWTOOLS_CEN/vortex-cen/configs/vortex_{server}.toml vortex.toml
